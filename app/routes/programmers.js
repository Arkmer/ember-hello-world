import Route from '@ember/routing/route';

export default Route.extend({
    model() {
        return ['Ryan Udelhoven', 'Louis Heim', 'Sam Standing', 'Christiana Routon', 'Elizabeth Wong']
    }
});
